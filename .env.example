# .env.example
# Copie este arquivo para ".env" e ajuste os valores conforme seu ambiente.
# Importante: NUNCA comite o arquivo ".env" com segredos reais.

########################################
# Ambiente LOCAL (sem Docker)
########################################
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=troque_esta_senha
DB_NAME=api_filmes
DB_SSLMODE=disable

########################################
# Ambiente DOCKER (com docker-compose)
# Obs.: O serviço do banco no docker-compose se chama "postgres"
# Se você for usar Docker, comente o bloco LOCAL acima e descomente este bloco.
########################################
# DB_HOST=postgres
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=troque_esta_senha
# DB_NAME=api_filmes
# DB_SSLMODE=disable

# Dicas de segurança:
# - Use uma senha forte em DB_PASSWORD (e mantenha-a igual em POSTGRES_PASSWORD e DB_PASSWORD quando usar Docker Compose).
# - Não compartilhe seu arquivo .env.
# - Utilize gerenciadores de segredos adequados no ambiente de produção.